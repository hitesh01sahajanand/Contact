<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="64dp"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="8dp"
    android:paddingEnd="8dp">

    <!-- Avatar (hidden by default; shown via code if available) -->
    <ImageView
        android:id="@+id/pop_image"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginEnd="10dp"
        android:visibility="gone" />

    <!-- Name + Number -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:gravity="start|center_vertical"
        android:orientation="vertical">

        <TextView
            android:id="@+id/pop_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="Unknown Number"
            android:textAppearance="@style/TextAppearance.Compat.Notification.Title"
            android:textSize="15sp" />

        <TextView
            android:id="@+id/pop_number"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:maxLines="1"
            android:ellipsize="end"
            android:text="+91 1234 456 456"
            android:textAppearance="@style/TextAppearance.Compat.Notification.Line2"
            android:textSize="12sp" />
    </LinearLayout>

    <!-- Decline button -->
    <ImageView
        android:id="@+id/pop_decline"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginStart="8dp"
        android:padding="4dp"
        android:src="@drawable/ic_call_reject" />

    <!-- Accept button -->
    <ImageView
        android:id="@+id/pop_accept"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginStart="8dp"
        android:padding="4dp"
        android:src="@drawable/ic_call_accept" />

</LinearLayout>